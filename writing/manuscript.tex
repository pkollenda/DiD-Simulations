%% (1) Set up document
\documentclass[
authoryear,
a4paper,
12pt,
oneside,
onccolumn
headings = normal
%draftmode = draft,
]{elsarticle}

%% Packages
% Disable elsevier footer
\makeatletter
\def\ps@pprintTitle{%
	\let\@oddhead\@empty
	\let\@evenhead\@empty
	\def\@oddfoot{}%
	\let\@evenfoot\@oddfoot}
\makeatother

% Page Setup
\usepackage[right=30mm,left=30mm,top=15mm,bottom=20mm, includeheadfoot]{geometry} % Setting page margins
\linespread{1.15} % Change to \linespread{1.15} for single spacing or  \linespread{1.66} for double spacing.
\usepackage[section]{placeins} % ensure that figures never get printed in a different section
%\let\Oldsubsection\subsection % same for subsections
%\renewcommand{\subsection}{\FloatBarrier\Oldsubsection}
%\usepackage{fancyhdr} % Support for header and footer options

% Language Support
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc} 	% utf-8 encoding
\usepackage[english]{babel} % english language support
\usepackage{lipsum} % allows filler text via \lipsum[1]
\usepackage[normalem]{ulem} % Adds easier underline commands

% Math Support
\usepackage{amsmath,  amssymb, amsfonts}

% Table Support
\usepackage{array, tabularx, booktabs}
%\usepackage{dcolumn} % Alignment on decimal. D{.}{.}{-1} would align on the decimal point, but this does not work if cells are wrapped in e.g. textbf{}
\usepackage{longtable, rotating, pdflscape} % Tables more than one page or landscape format
\usepackage{threeparttable, threeparttablex} % Nicer footnotes.
%\setlength{\tabcolsep}{-2pt}  % Decrease whitespace between table columns, use with caution!
%\usepackage{multirow} % Allows table cells which span more than one row
%\usepackage{colortbl} % Adds possibility to have table columns coloured
%\usepackage{makecell} % Allows fancier tables that had been created with R Markdown KableExtra
%\usepackage[tablesonly]{endfloat} % Puts all tables at the end of the document
%% Let longtable have a clearpage before to flush quede tables and assure longtable works with endfloat.
%\let\normallongtable=\longtable
%\renewcommand{\longtable}{\clearpage\normallongtable}	
%%\DeclareDelayedFloatFlavour*{longtable}{table}
%%\DeclareDelayedFloatFlavour*{landscape}{table}

% Figure Support
\usepackage{graphicx, subcaption}
%\usepackage{wrapfig} % wrapfigure and wraptable environments which place narrow floats at the edge of the text.
%\usepackage{float} % Add alternative floats, in boxes, placement, ...
\usepackage{xcolor} % More colors!

% References
% Allow backreferencing the references to where they appeared
\usepackage[backref=page, colorlinks, citecolor=blue]{hyperref} % Backref to add page numbers behind references that link back to where the reference was found. Renewcommand below adds a "[Cited on page X]" to reference list.
\renewcommand*{\backref}[1]{}
\renewcommand*{\backrefalt}[4]{%
	\ifcase #1 (Not cited.)%
	\or        [Cited on page~#2.]%
	\else      [Cited on pages~#2.]%
	\fi}
\usepackage[round]{natbib}

% Todo Package and Notes with Tcolorbox
\usepackage{tcolorbox}
\usepackage[disable, colorinlistoftodos, textsize=tiny]{todonotes} % Todo Comments; use option 'disable' to disable comments

%% Restart counter with Roman Numbering in Appendix
\let\origappendix\appendix % save the existing appendix command
\renewcommand\appendix{\clearpage\pagenumbering{Roman}\origappendix}


\begin{document}
% Cover & Titlepage
\input{./chapters/titlepage}

% Todolist
\listoftodos 

% Content Sections
\input{./chapters/introduction}


%% Bibliography (natbib, Ecta style-file)
\clearpage
\bibliographystyle{./references/aea}
%\bibliography{./References/library.bib}
\bibliography{/Users/pkollenda/Dropbox/PhilippEverywhere/TheLibrary/BibTex/library.bib}

% Appendix
\begin{appendix}	
	\include{./chapters/appendix/results_appendix}
\end{appendix}

\end{document}

